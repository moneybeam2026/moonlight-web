version: 1
frontend:
  phases:
    preBuild:
      commands:
        - node --version
        - npm --version
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist/frontend-app/browser
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
